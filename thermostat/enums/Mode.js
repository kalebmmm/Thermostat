module.exports = Object.freeze({
	AUTOMATIC: 'Automatic Control', // Will check for a device before turning heater on
	MANUAL: 'Manual Control' // Will keep at temperature whether device is present or not
});